{
  "name": "@are-visual/react-hooks",
  "version": "0.0.1",
  "main": "./index.ts",
  "scripts": {
    "gen-dts": "tsc --project tsconfig.build.json",
    "copy-dts": "copyfiles -u 1 \"./**/*.d.ts\" dist",
    "build-dts": "pnpm gen-dts && pnpm copy-dts"
  },
  "buildOptions": {
    "entry": "./index.ts",
    "postbuild": "build-dts"
  },
  "dependencies": {
    "@are-visual/portal-hosts": "workspace:^0.0.1",
    "@are-visual/utils": "workspace:^0.0.1"
  },
  "publishConfig": {
    "main": "index.js",
    "directory": "dist"
  }
}